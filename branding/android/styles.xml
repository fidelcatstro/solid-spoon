<?xml version="1.0" encoding="utf-8"?>
<!--
  S2000 Gauges — themed styles override.
  Copied into android/app/src/main/res/values/styles.xml by
  scripts/build-android.sh. Adds a translucent system-bar AppTheme so
  the WebView extends edge-to-edge under the (hidden) status / nav bars,
  and a dark splash theme so the launch screen never flashes white.
-->
<resources>
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:background">@android:color/black</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="AppTheme">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>
